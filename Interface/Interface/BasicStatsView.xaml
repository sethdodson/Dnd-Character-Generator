<UserControl x:Class="Interface.BasicStatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ViewModel="clr-namespace:Interface"
             mc:Ignorable="d" 
             d:DesignHeight="212" d:DesignWidth="492">
    <UserControl.DataContext>
        <ViewModel:CharacterViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Text="Character" Grid.Column="0" Grid.Row="0"/>
        <TextBox Text="{Binding Name, Mode=OneWayToSource}" Grid.Column="1" Grid.Row="0"/>
    </Grid>
</UserControl>
