<Window x:Class="Interface.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525" mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:my="clr-namespace:;assembly=DndCharacterGenerator" Loaded="Window_Loaded">
    <Window.Resources>
        <CollectionViewSource x:Key="agingViewSource" d:DesignSource="{d:DesignInstance my:Aging, CreateList=True}" />
    </Window.Resources>
    <Grid DataContext="{StaticResource agingViewSource}">
        <ListView Height="200" ItemsSource="{Binding}" Margin="136,120,-34,-9" Name="agingListView" SelectionMode="Single" Width="400">
            <ListView.ItemContainerStyle>
                <Style>
                    <Setter Property="Control.HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Control.VerticalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView />
            </ListView.View>
        </ListView>
    </Grid>
</Window>
